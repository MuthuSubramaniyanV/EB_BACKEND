from fastapi import APIRouter

router = APIRouter()

# TODO: Implement admin routes
# - GET /uploads/flagged (list flagged uploads)
# - PUT /uploads/{upload_id}/approve
# - PUT /uploads/{upload_id}/reject
# - GET /audit-log
